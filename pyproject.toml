[project]
name = "practical-ml-class"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pandas", "numpy", "scikit-learn", "matplotlib", "pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
