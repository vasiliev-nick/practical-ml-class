# OLD VERSION - do not use
# kept here in case the new one breaks
# (the new one is taxi_model_FINAL.py)
#
# import pandas as pd
# from sklearn.ensemble import RandomForestClassifier
#
# df = pd.read_csv("data.csv")
# df = df.dropna()
# X = df[["trip_distance", "fare_amount"]]
# y = (df["tip_amount"] > 2).astype(int)
# m = RandomForestClassifier()
# m.fit(X, y)
# print("ok")
#
# this version only used 2 features and didnt work as well
print("this is the old file, run taxi_model_FINAL.py instead")
